{
  "name": "pulumi-cloud",
  "private": true,
  "main": "index.ts",
  "description": "Pulumi Deployments settings and webhooks for the infra/ programs. Deliberately outside the infra/ npm workspace: it configures those programs rather than being one of them, and it is the only program here that depends on @pulumi/pulumiservice.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.257.0",
    "@pulumi/pulumiservice": "^1.3.0"
  },
  "//devDependencies": "Kept in lockstep with infra/package.json. typescript must stay below 7 for the same reason documented there.",
  "devDependencies": {
    "@types/node": "22.20.1",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  }
}
